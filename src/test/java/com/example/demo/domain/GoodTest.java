package com.example.demo.domain;

import static org.hamcrest.CoreMatchers.*;
import static org.junit.Assert.*;
import org.junit.Test;

public class GoodTest {
//    @Test
//    public void makeGood() {
//        Option option = new Option("red", Size.S, 10);
//        Shipping shipping = new Shipping(Method.PREPAY, 2500, false);
//        Good good = new Good("Spring", "styleshare", 10000, option, shipping);
//        assertThat(good.getOptions().size(), is(1));
//        assertThat(good.getShipping().getMethod(), is(Method.PREPAY));
//        assertThat(good.getOptions().get(0).getStock(), is(10));
//        assertThat(good.getOptions().get(0).getSize(), is(Size.S));
//    }
}
